Editor v2 (new version)

# Formo integration

Example:
```php
<?php

Formo::form('field', 'editor', array(
    'label' => 'My label',
    'editor' => 'Custom or Editor driver',
    'options' => array(
        'toolbar' => 'Full'
    )
));
```